import { QuantidadePipePipe } from './quantidade-pipe.pipe';

describe('QuantidadePipePipe', () => {
  it('create an instance', () => {
    const pipe = new QuantidadePipePipe();
    expect(pipe).toBeTruthy();
  });
});
