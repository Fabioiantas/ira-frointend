import { CommaNumberPipePipe } from './comma-number-pipe.pipe';

describe('CommaNumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CommaNumberPipePipe();
    expect(pipe).toBeTruthy();
  });
});
